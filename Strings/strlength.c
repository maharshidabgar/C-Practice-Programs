#include <stdio.h>
#include <string.h> // Standard Library Function for the Character

int main() {

    char name[] = "Mohit";
    int length = strlen(name);
    
    printf("Length is : %d",length);

    return 0;

}