#include <stdio.h>

int main() {

    int i = 1;

    // printf("%d \n", i++);  // use then increase - call this Post Increment Operator
    // printf("%d \n", i); // first print 1 after increase 1 and print 2

    printf("%d \n", ++i); // increase then use - call this Pre Increment Operator print direct 2

    return 0;
}