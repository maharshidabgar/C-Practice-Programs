#include <stdio.h>

int main() {

    int i = 2;

    // printf("%d \n", i--);  // use then decrease - call this Post Decrement Operator
    // printf("%d", i); // first print 2 after decrese 1 print

    printf("%d \n", --i); // only -- and ++ means decrease & increase 1 not 2 or any digit

    return 0;
}