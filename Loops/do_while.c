#include <stdio.h>

int main() {

    int i = 1;  // Initialization

    do {
        
        printf("%d \n", i);  // Some Work in Do

        i++;                // Updation

    } while (i <= 5); // Stoping = Termination

    return 0;
}