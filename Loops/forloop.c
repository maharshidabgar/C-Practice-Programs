#include <stdio.h>

int main() {

    for (int i = 0; i <= 10; i++) {  // (start, stop, incr/decr)
                                                                     // i = i + 1 same as i += 1 same as i++ ...
        printf("%d \n", i);   // i = iterator OR counter variable

    }

    return 0;
}