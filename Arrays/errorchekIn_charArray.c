#include <stdio.h>

int main() {

    //char *canChange = "Ram Ram";
    // puts(canChange);
    // canChange = "Jay Mataji";
    // puts(canChange);

    char cannotChange[] = "OK Boss";
    puts(cannotChange);

    cannotChange = "Jay Hoo";
    
    return 0;

}